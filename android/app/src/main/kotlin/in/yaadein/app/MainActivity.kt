package `in`.yaadein.app

import io.flutter.embedding.android.FlutterActivity

// Flutter handles all UI via the Dart engine; MainActivity is a thin wrapper.
// Do not add plugin registration here — Flutter's auto-registration handles it.
class MainActivity : FlutterActivity()
